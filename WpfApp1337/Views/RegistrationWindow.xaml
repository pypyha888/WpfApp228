<Window x:Class="ApplianceStoreIS.Views.RegistrationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Регистрация пользователя" Height="470" Width="520" MinWidth="480" MinHeight="420"
        WindowStartupLocation="CenterOwner" Icon="{StaticResource AppWindowIcon}">
    <Grid Margin="16" Background="#F4F6F8">
        <Border Background="White" CornerRadius="8" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="170"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="ФИО:" Margin="0,0,8,8" VerticalAlignment="Center"/>
                <TextBox x:Name="FullNameTextBox" Grid.Row="0" Grid.Column="1" Margin="0,0,0,8"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Логин:" Margin="0,0,8,8" VerticalAlignment="Center"/>
                <TextBox x:Name="LoginTextBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,8"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Пароль:" Margin="0,0,8,8" VerticalAlignment="Center"/>
                <PasswordBox x:Name="PasswordBox" Grid.Row="2" Grid.Column="1" Margin="0,0,0,8"/>

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Повторите пароль:" Margin="0,0,8,8" VerticalAlignment="Center"/>
                <PasswordBox x:Name="ConfirmPasswordBox" Grid.Row="3" Grid.Column="1" Margin="0,0,0,8"/>

                <TextBlock Grid.Row="4" Grid.Column="0" Text="Роль:" Margin="0,0,8,8" VerticalAlignment="Center"/>
                <ComboBox x:Name="RoleComboBox" Grid.Row="4" Grid.Column="1" Margin="0,0,0,8" SelectedIndex="0">
                    <ComboBoxItem Content="Пользователь" Tag="User"/>
                    <ComboBoxItem Content="Менеджер" Tag="Manager"/>
                    <ComboBoxItem Content="Администратор" Tag="Admin"/>
                </ComboBox>

                <TextBlock Grid.Row="5" Grid.Column="0" Text="Спец-код роли:" Margin="0,0,8,8" VerticalAlignment="Center"/>
                <PasswordBox x:Name="PrivilegedCodePasswordBox" Grid.Row="5" Grid.Column="1" Margin="0,0,0,8"/>

                <TextBlock Grid.Row="6" Grid.ColumnSpan="2" Foreground="#666" Margin="0,0,0,8" Text="Для ролей Менеджер/Администратор требуется специальный код."/>

                <Button Grid.Row="7" Grid.ColumnSpan="2" Content="Зарегистрировать" Height="32" Margin="0,10,0,0" Click="OnRegisterClick"/>
            </Grid>
        </Border>
    </Grid>
</Window>
