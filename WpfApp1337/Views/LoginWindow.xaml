<Window x:Class="ApplianceStoreIS.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Авторизация — Магазин бытовой техники" Height="420" Width="560" MinWidth="500" MinHeight="360"
        WindowStartupLocation="CenterScreen" Icon="{StaticResource AppWindowIcon}">
    <Grid Background="#F4F6F8" Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12" VerticalAlignment="Center">
            <Border Width="50" Height="50" Margin="0,0,12,0" Background="#2C7BE5" CornerRadius="8"><TextBlock Text="BT" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="18"/></Border>
            <TextBlock Text="ИС «Магазин бытовой техники»" FontSize="22" FontWeight="Bold" VerticalAlignment="Center"/>
        </StackPanel>

        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="24">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="130"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Логин:" Grid.Row="0" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center"/>
                <TextBox x:Name="LoginTextBox" Grid.Row="0" Grid.Column="1" Margin="0,0,0,8" MinHeight="28"/>

                <TextBlock Text="Пароль:" Grid.Row="1" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center"/>
                <PasswordBox x:Name="PasswordBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,8" MinHeight="28"/>

                <Button Grid.Row="2" Grid.ColumnSpan="2" Content="Войти" Height="32" Margin="0,8,0,0" Click="OnLoginClick"/>

                <TextBlock Grid.Row="3" Grid.ColumnSpan="2" Margin="0,14,0,6" Foreground="#666" Text="Создайте аккаунт через кнопку «Регистрация»."/>
            </Grid>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <TextBlock Text="Нет аккаунта?" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <Button Content="Регистрация" Width="120" Click="OnOpenRegistrationClick"/>
        </StackPanel>
    </Grid>
</Window>
